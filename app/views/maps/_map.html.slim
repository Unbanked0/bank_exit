#map.w-full.z-0.relative class="#{defined?(from_welcome) && from_welcome ? 'h-[600px]' : 'h-[400px] lg:h-full'}" data-controller="map" data-map-markers-value=@merchant_markers.to_json data-map-zoom-value=@zoom data-map-latitude-value=@latitude data-map-longitude-value=@longitude data-map-track-geolocation-in-url-value=(defined?(from_welcome) && from_welcome ? 'false' : 'true') data-map-referer-url-value=maps_referer_url data-action="map--options:refreshMap@window->map#refreshMap" data-map-attribution-html-value=html_escape_once(map_attribution_html)
  = render 'maps/loader'
