header.fixed.max-w-screen-3xl.w-full.print:hidden.bg-base-100/80.border-b-2.border-primary.motion-safe:transition-colors class=('z-[9999]') class=('bg-base-100!' unless welcome_page?) data-layout-target=("header" if welcome_page?)
  #flashes= render 'flashes'
  = render 'nav', projects: @projects
